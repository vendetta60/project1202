# RBAC System Architecture & Data Flow Diagrams

## 1. Permission Assignment Hierarchy

```
                        SYSTEM ROLES
                    (Created by System)
                    ┌─────────────────┐
                    │  System Admin    │  ← All Permissions
                    │ Appeals Manager  │  ← Appeals-related perms
                    │ Report Analyst   │  ← Report-related perms
                    │    Viewer        │  ← Read-only perms
                    └────────┬─────────┘
                             │
                             │
                    ┌────────▼─────────┐
                    │   ASSIGN TO      │
                    │     USERS        │
                    └────────┬─────────┘
                             │
                    ┌────────▼──────────────────────┐
                    │   USER PERMISSIONS           │
                    │  (Aggregated from all roles) │
                    │  (± Individual overrides)     │
                    └─────────────────────────────┘
```

---

## 2. Role → Permissions → User Access

```
                    ┌──────────────┐
                    │ Role Created │
                    └──────┬───────┘
                           │
                   ┌───────▼────────┐
                   │ Add Permissions│ (e.g., view_appeals, create_appeal)
                   └───────┬────────┘
                           │
                   ┌───────▼────────┐
                   │  Assign to User│ (User now has all role permissions)
                   └───────┬────────┘
                           │
                   ┌───────▼────────────────┐
                   │ User Gets Permissions  │
                   │ from role automatically│
                   └────────────────────────┘
```

---

## 3. Permission Template Application

```
TEMPLATE SETUP          TEMPLATE APPLICATION         RESULT
─────────────           ──────────────────           ──────

Appeals Manager    +    Select User         →    User Gets:
Group Template            Click Template          • view_appeals
contains:                                        • create_appeal
• view_appeals                                   • edit_appeal
• create_appeal                                  • assign_appeal
• edit_appeal                                    • export_appeals
• assign_appeal                                  • + 4 more
• export_appeals
• ...                                      (All permissions 
                                           from template)
```

---

## 4. Permission Resolution Algorithm

```
GET USER PERMISSIONS:

Step 1: Collect from Roles
─────────────────────────────
FOR each user_role IN user.roles:
    FOR each permission IN role.permissions:
        user_permissions.add(permission)

Step 2: Apply Individual Overrides
──────────────────────────────────
FOR each individual_perm IN user.permissions:
    IF individual_perm.type == "GRANT":
        user_permissions.add(permission)
    ELSE IF individual_perm.type == "DENY":
        user_permissions.remove(permission)

Step 3: Return Final Permissions
─────────────────────────────────
RETURN user_permissions
```

**Example:**
```
User "Ahmed" has:
  - Role: Appeals Manager (grants: view_appeals, create_appeal)
  - Individual: GRANT export_appeal
  - Individual: DENY create_appeal

Final Permissions:
  view_appeals ✓      (from role)
  create_appeal ✗     (revoked by individual deny)
  export_appeal ✓     (from individual grant)
```

---

## 5. Database Schema Relationships

```
                    ┌──────────────┐
                    │   Users      │
                    ├──────────────┤
                    │ id (PK)      │
                    │ username     │
                    │ ...          │
                    └──────┬───────┘
                           │
            ┌──────────────┴──────────────┐
            │                             │
    ┌───────▼─────────┐        ┌────────▼───────────┐
    │  UserRoles      │        │ UserPermissions    │
    ├─────────────────┤        ├──────────────────┐
    │ user_id (FK)    │        │ user_id (FK)     │
    │ role_id (FK)    │        │ permission_id(FK)│
    │                 │        │ grant_type       │
    └────────┬────────┘        └──────────────────┘
             │
    ┌────────▼────────┐
    │  Roles          │
    ├─────────────────┤
    │ id (PK)         │
    │ name            │
    │ is_system       │
    │ ...             │
    └────────┬────────┘
             │
    ┌────────▼──────────────┐
    │ RolePermissions       │
    ├───────────────────────┤
    │ role_id (FK)          │
    │ permission_id (FK)    │
    └────────┬──────────────┘
             │
    ┌────────▼─────────────┐
    │   Permissions        │
    ├──────────────────────┤
    │ id (PK)              │
    │ code (e.g., "view")  │
    │ name (e.g., "View")  │
    │ category (e.g., app) │
    │ ...                  │
    └──────────────────────┘


PermissionGroups Relationship:
───────────────────────────────
    ┌─────────────────────┐
    │ PermissionGroups    │
    ├─────────────────────┤
    │ id (PK)             │
    │ name                │
    │ is_template         │
    │ ...                 │
    └──────────┬──────────┘
               │
    ┌──────────▼──────────────┐
    │ PermissionGroupItems    │
    ├─────────────────────────┤
    │ group_id (FK)           │
    │ permission_id (FK)      │
    └────────────────────────┘
```

---

## 6. API Endpoint Flow

```
FRONTEND REQUEST                  BACKEND PROCESSING              DATABASE
────────────────                 ──────────────────              ──────────

User clicks           →  POST /api/v1/permissions/  →  PermissionService
"Assign Role"             users/{id}/roles/{rid}        .assign_role()
                                                              │
                                                     UserRoleRepository
                                                       .assign_role()
                                                              │
                         ◄──────────────────────  INSERT INTO UserRoles
                                                        (user_id, role_id)

Response: {"status": "success"}

Next: Frontend fetches         GET /api/v1/permissions/  →  UserPermissionService
      user permissions         users/{id}/permissions        .get_permissions()
                                                              │
                                              Query user's roles & permissions
                                                              │
Response: {                    ◄──────────────────  Returns aggregated
  permission_codes: [...],                        permission codes from:
  role_ids: [...]                                 1. User's roles
}                                                 2. Individual overrides
```

---

## 7. User Permission Resolution Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER LOGS IN and Requests Protected Resource                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    ┌────────▼─────────┐
                    │ Check Endpoint   │
                    │ Requires Perm?   │
                    └────────┬─────────┘
                             │
                    ┌────────▼──────────────────┐
                    │ Get User Object from DB  │
                    └────────┬──────────────────┘
                             │
            ┌────────────────┴────────────────┐
            │                                 │
    ┌───────▼────────┐            ┌─────────▼──────────┐
    │ Get All Roles  │            │ Get Individual     │
    │ for User       │            │ Permission Grants  │
    └────────┬───────┘            │ / Denies           │
             │                    └─────────┬──────────┘
             │                              │
    ┌────────▼──────────────┐      ┌───────▼─────────┐
    │ Collect Permissions   │      │ Apply Overrides │
    │ from All Roles        │      │ (+/-)           │
    └────────┬──────────────┘      └───────┬─────────┘
             │                              │
             └──────────────┬───────────────┘
                           │
                  ┌────────▼─────────┐
                  │ Final Permission │
                  │ Set for User     │
                  └────────┬─────────┘
                           │
                    ┌──────▼──────────┐
                    │ Check if User   │  ├─◄ YES → Allow Access
                    │ has Required?   │  │ Required Permission ✓
                    │ Permission      │  │
                    │                 │  ├─◄ NO → Deny Access
                    │                 │  └ Permission Missing ✗
                    └─────────────────┘
```

---

## 8. Admin Panel Workflow

```
┌─────────────────────────────────────────────────────────┐
│        ADMIN LOGS IN                                    │
│   (has access_admin_panel permission)                   │
└────────────────┬────────────────────────────────────────┘
                 │
        ┌────────▼─────────┐
        │  CHOOSE TAB       │
        └────────┬─────────┘
                 │
         ┌───────┴──────────┬──────────────┬──────────────┐
         │                  │              │              │
    ┌────▼────┐      ┌─────▼──────┐  ┌───▼────┐  ┌──────▼─────┐
    │Permissions   │Roles         │ │Groups   │  │User Perms   │
    │Management │ Management     │ │Templates│  │Management   │
    └──────────┘      └────────────┘  └────────┘  └─────────────┘
         │                  │              │              │
         │                  │              │              │
    ┌────▼────────────────────────────────────────────────▼────┐
    │                  API CALLS                               │
    │  POST /api/v1/permissions/create                        │
    │  POST /api/v1/permissions/roles/create                 │
    │  POST /api/v1/permissions/groups/create                │
    │  POST /api/v1/permissions/users/{id}/roles/{rid}       │
    │  ... and many more                                      │
    └──────┬─────────────────────────────────────────────────┘
           │
           │
    ┌──────▼─────────────────────────────┐
    │  BACKEND PROCESSING                │
    │  (Repository → Service → Model)    │
    └──────┬─────────────────────────────┘
           │
           │
    ┌──────▼──────────────────────────┐
    │  DATABASE UPDATES                │
    │  (INSERT/UPDATE/SELECT)          │
    └──────┬───────────────────────────┘
           │
           │
    ┌──────▼──────────────────────────┐
    │  RETURN SUCCESS RESPONSE         │
    │  (with updated data)             │
    └──────┬───────────────────────────┘
           │
    ┌──────▼──────────────────────────┐
    │  FRONTEND UPDATES UI             │
    │  (shows new permissions/roles)   │
    └──────────────────────────────────┘
```

---

## 9. Permission Category Hierarchy

```
PERMISSIONS ORGANIZED BY CATEGORY:

Appeals Mgmt          Users Mgmt              Reports              Audit
──────────           ──────────              ───────              ─────
view_appeals         view_users              view_reports         view_audit_logs
create_appeal        create_user             create_report        export_audit_logs
edit_appeal          edit_user               export_report
delete_appeal        delete_user             manage_templates
view_details         manage_roles
assign_appeal        manage_perms
complete_appeal      reset_password
export_appeals

                                              Citizens              Admin
                                              ───────              ─────
                                              view_citizens        manage_roles
                                              create_citizen       manage_permissions
                                              edit_citizen         manage_groups
                                              delete_citizen       access_admin_panel
                                                                   system_configuration
```

---

## 10. Complete User Setup Flow

```
NEW EMPLOYEE JOINS
    │
    ▼
  STEP 1: CREATE EMPLOYEE USER ACCOUNT
  ┌────────────────────────────────────┐
  │ Admin goes to Users section         │
  │ Creates new user with credentials  │
  └────────────┬───────────────────────┘
               │
  STEP 2: DETERMINE ROLE
  ┌────────────▼───────────────────────┐
  │ What is employee's job title?       │
  │ • Appeals Manager                   │
  │ • Report Analyst                    │
  │ • Custom Role                       │
  └────────────┬───────────────────────┘
               │
  STEP 3: ASSIGN ROLE (Option A: Manual)
  ┌────────────▼───────────────────────┐
  │ Go to User Permissions              │
  │ Select Employee                     │
  │ Click "Add Role"                    │
  │ Choose role (e.g., Appeals Manager) │
  │ Click "Assign"                      │
  └────────────┬───────────────────────┘
               │
  STEP 3b: APPLY TEMPLATE (Option B: Quick)
  ┌────────────▼───────────────────────┐
  │ Go to User Permissions              │
  │ Select Employee                     │
  │ Click Template Button               │
  │ (e.g., "Appeals Manager Group")    │
  │ = Gets all permissions at once      │
  └────────────┬───────────────────────┘
               │
  STEP 4: VERIFY PERMISSIONS
  ┌────────────▼───────────────────────┐
  │ View "Current Permissions" section  │
  │ Confirm all expected permissions    │
  │ Are there correctly listed           │
  └────────────┬───────────────────────┘
               │
  STEP 5: NOTIFY EMPLOYEE
  ┌────────────▼───────────────────────┐
  │ Employee can log in                 │
  │ Has access to assigned features     │
  │ ✓ Ready to work!                    │
  └────────────────────────────────────┘
```

---

## 11. Quick Reference Symbols

```
🔐 = Requires admin/special permission
👥 = Affects multiple users
📦 = Permission template/group
📝 = Create/modify operation
🗑️  = Delete operation
✓ = Granted/Active
✗ = Denied/Inactive
→ = Data flow/Assignment
← = Response/Return
┌─┐ = Table/Container
│ │ = Table contents
└─┘
```

---

**This diagram set helps visualize how the RBAC system works!**
